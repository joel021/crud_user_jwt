version: '3.1'
services:

  mysql-standalone:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=default_mysql_docker_pass
      - MYSQL_PASSWORD=default_mysql_docker_pass
      - MYSQL_DATABASE=crud_user_db
    ports:
      - "3307:3307"

  user-crud-application-container:
    image: user-crud-application
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql-standalone:3306/crud_user_db
      SPRING_DATASOURCE_USERNAME: "root"
      STRING_DATASOURCE_PASSWORD: "default_mysql_docker_pass"
    build:
      context: "./"
      dockerfile: "Dockerfile"
    depends_on:
      - mysql-standalone