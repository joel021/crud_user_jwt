export default class RoleAuthority {

    static readonly ADMIN = 'ADMIN';
    static readonly USER = 'USER';

    static readonly PERMISSION_TYPES = [
        RoleAuthority.ADMIN,
        RoleAuthority.USER
    ];

}
